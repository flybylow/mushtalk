# docs/

Internal notes for mushtalk. The root stays a working project (`README.md` plus code). Everything else that we need to remember lives here.

- Names: `kebab-case.md`
- `INDEX.md` is the table of contents — update it when a file is added, removed, or renamed.
- `BASE.md` is this contract.
